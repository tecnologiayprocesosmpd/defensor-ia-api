{
  "name": "saepbi-api",
  "version": "1.0.0",
  "description": "API REST para consultar datos de SAEPBI",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "if not exist dist mkdir dist && copy index.js dist\\ && copy package.json dist\\ && copy package-lock.json dist\\ && copy .env dist\\"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}