server {
    listen 80;
    server_name web-intranet.mpdtucuman.gob.ar;

    # Frontend producción
    location / {
        root /var/www/web-intranet/defensor-ia/frontend;
        try_files $uri $uri/ /index.html;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Frontend pruebas
    location /test/ {
        alias /var/www/web-intranet-test/defensor-ia/frontend/;
        index index.html;
        try_files $uri $uri/ /test/index.html;
        # NO usar caché largo ni immutable en pruebas
        expires 0;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    location = /test/index.html {
        alias /var/www/web-intranet-test/defensor-ia/frontend/index.html;
    }

    # Bloquear archivos ocultos
    location ~ /\. {
        deny all;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name web-intranet.mpdtucuman.gob.ar;

    ssl_certificate /etc/ssl/certs/_.mpd.crt;
    ssl_certificate_key /etc/ssl/private/_.mpd.key;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # === FRONTEND PRODUCCIÓN ===
    location / {
        root /var/www/web-intranet/defensor-ia/frontend;
        index index.html;
        try_files $uri $uri/ /index.html;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # === FRONTEND PRUEBAS ===
    location /test/ {
        alias /var/www/web-intranet-test/defensor-ia/frontend/;
        index index.html;
        try_files $uri $uri/ /test/index.html;
        # NO usar caché largo ni immutable en pruebas
        expires 0;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    location = /test/index.html {
        alias /var/www/web-intranet-test/defensor-ia/frontend/index.html;
    }

    # === API PRUEBAS: EXCEPCIONES ESPECÍFICAS (deben ir ANTES de la regla general) ===
    location ^~ /test/api/GetPermisos {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /test/api/GetParamSistCarcelario {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /test/api/EditParamSistCarcelario {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /test/oauth/ {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar/oauth/;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # === API PRUEBAS: REGLA GENERAL (DEBE IR DESPUÉS DE LAS EXCEPCIONES) ===
    location ^~ /test/api/ {
        proxy_pass http://localhost:6000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # === API PRODUCCIÓN: EXCEPCIONES ===
    location ^~ /api/GetPermisos {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /api/GetParamSistCarcelario {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /api/EditParamSistCarcelario {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar;
        proxy_http_version 1.1;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_ssl_verify off;
    }

    location ^~ /oauth/ {
        proxy_pass https://portaldelempleado.mpdtucuman.gob.ar/oauth/;
        proxy_ssl_server_name on;
        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_set_header Host portaldelempleado.mpdtucuman.gob.ar;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # === API PRODUCCIÓN: REGLA GENERAL ===
    # CORREGIDO: Agregado /api/ al final del proxy_pass
    location ^~ /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # === SEGURIDAD ===
    location ~ /\. {
        deny all;
    }
}
